
<div class="orgs-list">
    {{#each model as |repo|}}
        {{github-repo
            gh-repo-data = repo
            gh-link-route = 'org.repo.issues'            
        }}            
    {{/each}}
</div>
{{outlet}}